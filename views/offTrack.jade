extends layout

block content

	if message
		p.pull-center.message #{message}
	if error
		p.pull-center.error #{error}

	#main.pure-g-r
		.pure-u-1-3
		.pure-u-1-3.pull-center
			h1.thin You are off-track
		.pure-u-1-3
		.pure-u-1-2.pull-center
			h2
				a(href="/off-track/accounts") #{accounts.length} Accounts
		.pure-u-1-2.pull-center
			h2
				a(href="/off-track/categories") #{categories.length} Categories